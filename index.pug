

doctype html
html
  head
    title Announcement 
    style(type="text/css").
      body {
        text-align: center;
      }
  body
    h1 Announcement
    p Message: #{message}
    form(method='POST' action='/acknowledge')
      button.btn.btn-primary(type='submit') Acknowledge

