

doctype html
html
  head
    title Announcement 
    style(type="text/css").
      body {
        text-align: center;
      }
  body
    h1 Announcement
    p #{message}
    form(method='POST' action='/acknowledge')
      button.btn.btn-primary(type='submit') Acknowledge

